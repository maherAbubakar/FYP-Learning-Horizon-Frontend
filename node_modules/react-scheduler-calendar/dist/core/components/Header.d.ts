import moment from 'moment';
import React, { ReactNode } from 'react';
declare type noopType = (args: any) => any;
interface HeaderViewConfig {
    viewType: number;
    showAgenda: boolean;
    isEventPerspective: boolean;
    dateLabel: string;
}
export interface Props {
    title?: string | ReactNode;
    onViewChange: noopType;
    goToToday: noopType;
    goBack: noopType;
    goNext: noopType;
    onSelect: noopType;
    rightCustomHeader?: string | ReactNode;
    selectDate: string;
    localeMoment: typeof moment;
    headerGroupView: HeaderViewConfig;
    config: any;
}
interface State {
    visible: boolean;
}
declare class Header extends React.Component<Props, State> {
    constructor(props: Props);
    renderRadioButtonList(config: any): any;
    renderPopover: () => JSX.Element;
    handleVisibleChange: (visible: boolean) => void;
    render(): JSX.Element;
}
export default Header;

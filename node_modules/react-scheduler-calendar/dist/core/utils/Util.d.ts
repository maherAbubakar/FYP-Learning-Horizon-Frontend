import { Moment } from 'moment';
export declare const getPos: (element: HTMLElement | null) => {
    x: number;
    y: number;
};
export declare const getScrollSpeedRate: (distance: number, maxDistance: number) => number;
export declare const customFloorUnit: (time: string | Moment, unitConfig: number) => string;
